{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC to the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
