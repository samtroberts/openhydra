# ── Core runtime ─────────────────────────────────────────────────────
torch>=2.0
grpcio>=1.78
grpcio-tools>=1.78
protobuf>=4.25,<7
transformers>=5.5.0
accelerate>=1.13.0
requests>=2.28.0
sentencepiece>=0.2.0
cryptography>=42,<47

# ── Apple Silicon (MLX) — see requirements-mlx.txt ───────────────────
# pip install -r requirements-mlx.txt

# ── Development / test ───────────────────────────────────────────────
pytest>=8.0,<9
pytest-asyncio>=0.23
